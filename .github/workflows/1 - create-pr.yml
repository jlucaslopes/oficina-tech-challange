name: 1 - create-pr.yml

on:
  push:
    branches:
      - 'feat**'
permissions:
  contents: write
  pull-requests: write

jobs:
  build-test-install:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar JDK
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Executar mvn test
        run: mvn test --batch-mode

      - name: Executar mvn install
        run: mvn install --batch-mode -DskipTests=true

  create-pr:
    needs: build-test-install
    runs-on: ubuntu-latest

    steps:
      - name: Criar Pull Request manualmente via API
        if: ${{ fromJson(steps.check_pr.outputs.data) == '[]' || fromJson(steps.check_pr.outputs.data) == null || (fromJson(steps.check_pr.outputs.data) | length) == 0 }}
        env:
          GH_TOKEN: ${{ secrets.PAT_GITHUB }}
          HEAD_BRANCH: ${{ github.ref_name }}
          BASE_BRANCH: develop
          REPO: ${{ github.repository }}
        run: |
          echo "🛠️ Criando Pull Request manualmente de ${HEAD_BRANCH} → ${BASE_BRANCH}"
          API_URL="https://api.github.com/repos/${REPO}/pulls"
          TITLE="Feature → Develop: ${HEAD_BRANCH}"
          BODY="Pull request automático criado a partir da branch **${HEAD_BRANCH}**.  
          - Build e testes executados com sucesso ✅"

          RESPONSE=$(curl -s -X POST -H "Authorization: token ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            ${API_URL} \
            -d "$(jq -n --arg title "$TITLE" --arg head "$HEAD_BRANCH" --arg base "$BASE_BRANCH" --arg body "$BODY" \
            '{title: $title, head: $head, base: $base, body: $body}')")

          PR_URL=$(echo "$RESPONSE" | jq -r '.html_url')
          if [ "$PR_URL" = "null" ] || [ -z "$PR_URL" ]; then
            echo "❌ Falha ao criar o Pull Request"
            echo "$RESPONSE"
            exit 1
          else
            echo "✅ Pull Request criado com sucesso: $PR_URL"
          fi
